[project]
name = "inference-energy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27.0", "pynvml>=11.5.0"]

[project.scripts]
inference-energy = "inference_energy.cli:main"
